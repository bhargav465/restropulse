# @env tag-format: owner=<component|shared|root|unknown> <required|optional> <runtime|buildtime> <secret|public> state=<active|stale|orphan|workflow-only>

# ngrok tunnel for local Razorpay webhook testing
# Get your auth token at: https://dashboard.ngrok.com/authtokens
# Get a free static domain at: https://dashboard.ngrok.com/domains
# @env owner=root optional runtime secret state=active
NGROK_AUTH_TOKEN=
# @env owner=root optional runtime public state=active
NGROK_DOMAIN=postlike-overdiscriminatingly-jalisa.ngrok-free.dev
