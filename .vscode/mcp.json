{
  "mcpServers": {
    "ag2-whatsapp-business-api": {
      "command": "npx",
      "args": ["-y", "@ag2/whatsapp-business-api"],
      "env": {
        "WHATSAPP_API_TOKEN": "${env:WHATSAPP_ACCESS_TOKEN}",
        "WHATSAPP_PHONE_NUMBER_ID": "${env:WHATSAPP_PHONE_NUMBER_ID}"
      }
    }
  }
}
